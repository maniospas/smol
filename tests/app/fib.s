@include std.core
@include std.mem

def fib(@mut u64[] ret, @mut u64[] args, u64 n)
    if n<=1
        ret.push()
        stack.push(n-1)
        stack.push(n-2)

service main()
    @mut args = u64[]
    @mut ret = u64[]
    @mut top = 0
    while top<stack.len()
        stack.

    print(ret[0])
    
    

